package library.clientCommands.commandType;

import library.clientCommands.Command;
import library.clientCommands.UserData;

public class LogCommand extends Command {

    public LogCommand(UserData userData) {
        super(userData);
    }
}
